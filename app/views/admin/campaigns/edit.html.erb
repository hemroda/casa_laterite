<div class="w-full md:flex justify-between items-center mt-2 p-3 bg-slate-900 text-slate-300">
  <div>
    <h2 class="font-bold text-2xl"><%= @campaign.name %></h2>
  </div>
  <div class="flex">
    <%= link_to "Back to campaigns list", admin_campaigns_path, class: "mr-4 hover:text-orange-800" %>
    <%= link_to "Work with this campaigns", admin_campaign_path(@campaign), class: "hover:text-orange-800" %>
  </div>
</div>
<div class="bg-slate-900 p-8 pt-7 mt-3">
  <div class="mt-4"><%= render "form" %></div>
</div>
