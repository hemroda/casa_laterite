<%= form_with(model: [ :admin, @commentable, object ], local: true) do |f| %>
  <div>
    <%= f.rich_text_area :content, class: "basic-field" %>
  </div>

  <%= f.button :submit, class: "submit-btn bg-orange-900" %>
<% end %>
