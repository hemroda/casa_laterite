<div class="w-full md:flex justify-between items-center mt-2 p-3 bg-slate-900 text-slate-300">
  <div><h2 class="font-bold text-2xl"><%= @deck.name %></h2></div>
  <div class="flex">
    <%= link_to "Back to decks list", admin_decks_path, class: "mr-4 hover:text-orange-800" %>
    <%= link_to "Work with this decks", admin_deck_path(@deck), class: "hover:text-orange-800" %>
  </div>
</div>
<div class="bg-slate-900 p-8 pt-7 mt-3">
  <div class="mt-4"><%= render "form" %></div>
</div>
